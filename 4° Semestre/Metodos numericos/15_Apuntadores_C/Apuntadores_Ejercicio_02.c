#include <stdio.h>
#include <stdlib.h>

int main(void)
{
int n, c;
int*i;

printf("Ingresa un valor: ");
scanf("%i", &n);

i=(int*)malloc((i+1)*sizeof(int));
if(buffer==NULL)exit(1);

for(c=0;c<n;c++){
printf("Ingresa un valor: ");
scanf("%i", &i[c]);
}
for(c=0;c<n;c++){
printf("valor%d: %d\n", c, i[c]);
}
  
free(c);
free(n);
free(i)
system("Pause");
}
